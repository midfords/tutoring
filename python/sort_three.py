# 
# Sort Three
#
# Python tutoring exercise.
# Sean Midford, 2020
#

def sort(a, b, c):
    """This function takes three numbers a, b and c and returns a list
    of the sorted numbers.

    Example 1: 1, 2, 3 -> [1, 2, 3]  The three numbers are 1, 2 and 3 so the
        sorted list is [1, 2, 3]
    Example 2: [5, -2, 4] -> [-2, 4, 5]  The three numbers are 5, -2 and 4 so the
        sorted list is [-2, 4, 5]
    Example 3: 0, 0, 0 -> [0, 0, 0]  All three numbers are 0, so the sorted list
        is [0, 0, 0]

    Parameters
    ----------
    a : int
        First number to be sorted.
    b : int
        Second number to be sorted.
    c : int
        Third number to be sorted.
    
    Returns
    -------
    list
        Return the list of sorted numbers.
    """
    pass


if __name__ == "__main__":
    print("Do not run this file directly. \nRun the test file instead: python sort_three.test.py")