# 
# Complete
#
# Python tutoring exercise.
# Sean Midford, 2020
#

def isComplete(l):
    """This function takes a list l, and checks if it contains every integer between 1
    and the length of the list. The given list l can be in any order.
    An empty list should always return true.
    Example 1: [4,3,1,2] -> True  because the list of 4 items contains 1, 2, 3 and 4
    Example 2: [] -> True  because an empty list always returns true
    Example 3: [5,1,3,2] -> False  because a list of 4 numbers should contain 1, 2, 3 and 4, not 1, 2, 3 and 5

    Parameters
    ----------
    l : list
        The list of integers to be checked.
    
    Returns
    -------
    bool
        True if each number is present, otherwise False.
    """
    pass


if __name__ == "__main__":
    print("Do not run this file directly. \nRun the test file instead: python complete.test.py")